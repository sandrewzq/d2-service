# 当前待办

> 更新时间：2026-09-07
> 本文件只保留当前任务、状态和下一步。详细范围与验收标准由对应 backlog 维护，阶段过程使用 Git 历史追溯。

## 健康度

| 检查项 | 状态 | 备注 |
|---|---|---|
| 本地 CI | ✅ 2026-09-07 通过 | 已通过文档、编码、全部 workspace 构建、492 项行为测试、质量门禁、62 项架构测试、UI 合同、共享 Shell 视觉合同和全量类型检查 |
| GitHub CI | ⏳ push 后异步执行 | 执行 frozen install、`pnpm test`、共享 Shell 视觉契约、UI 合同静态门禁和构建后的 `pnpm typecheck:ci`；普通 push 不等待结果 |
| Release 门禁 | ✅ 0.0.20 已发布 | 已通过 macOS 发布入口完成本地门禁、GitHub Actions、Windows NSIS 安装包和 GitHub Release |
| Agent 自动验证 | ⛔ 默认禁用 | 只有用户明确要求本地测试、构建或打包时才执行 |

## 当前任务

| 编号 | 优先级 | 状态 | 任务 | Backlog | 下一步 |
|---|---|---|---|---|---|
| T14 | P1 | ✅ 开发完成，待整体验收 | 应用配装主流程简化与逐部位属性模组 | [任务说明](work/backlog/T14-armor-planner-slot-aware-mods.md) | 护甲规划已收敛为“配装设置 / 推荐方案”两栏；五部位可分别直选自动、不装、+5 或 +10，固定数值后再选属性；优先级改为有序编辑，技能与碎片、规划方式、套装和装备范围收入更多条件；下一步在 `574 × 832`、`760px`、`980px` 和 `1280px` 实窗验收控件不溢出、普通路径无技术术语、优先顺序可见且键盘可操作 |
| T20 | P1 | 🟡 S0–S18、S21–S23 当前范围完成，S19–S20 分阶段推进 | 武器推荐严格语义、核心 Roll、缓存刷新与仓库整理 | [任务说明](work/backlog/T20-weapon-recommendation-vault-cleanup.md) | 仓库浏览收敛为工作流导航和单条结果命令栏；搜索、来源、其他状态与完整命中移除重复字段标题，核心命中改为“命中 x/y + 独立数量徽标”。左侧范围与普通条件使用连续分段结构，查看范围使用连续单选列表，游戏事实筛选保留低饱和语义色；重置入口固定在左侧搜索行。共享主操作改用操作蓝，绿色只表达成功和符合；卡片“加锁 / 解锁”降为安静操作，“取出”保留中性描边。下一步在 `light / dark × 1280 / 980 / 760` 下实窗核对边框层级、选中与焦点状态、命中文案、单栏不溢出和批量模式展开稳定。 |
| Bug #13 | P1 | ✅ 已修复，待 0.0.18 验收 | 旧版本更新后应用进程 / 窗口不显示 | — | 配置读取恢复旧字段默认值；启动异常改为弹窗提示；单实例在窗口尚未创建时会尝试补建窗口 |
| Bug #20 | P1 | 🟡 已修复，待网络异常回归 | DIM 社区推荐在线检查网络异常时，弹窗长时间停留在“检查并读取中”，且关闭按钮与 Escape 同时被禁用，用户看起来像被卡在弹窗内 | 在线检查改为后台非阻塞；commit 检查超时 30 秒、Wishlist 下载超时 180 秒；UI 增加 210 秒兜底并提供本地文件回退提示；检查期间可继续使用其他入口或关闭弹窗，确认更新和本地导入仍保持阻塞保护 | 用断网、慢网和请求超时场景确认弹窗可关闭、其他入口可操作、检查完成后状态自动更新，当前推荐数据不被清空 |
| Bug #21 | P1 | ✅ 已修复，待旧版本升级验收 | 旧客户端升级后资料库更新缺少真实进度与可用性反馈，用户无法判断下载缓慢、索引处理中还是任务卡住 | — | 已增加流式下载量、中英文下载与索引阶段、旧库可用性和重试信息；应用更新与资料库更新现共用统一进度组件，顶部、全局 Dock、设置页和资料库页按同一状态语义展示阶段、百分比、下载量、速度及重试信息；旧索引按实际兼容性继续使用，确需重建时可复用同版本主 SQLite，最后切换期间查询自动等待恢复。下一步使用旧 schema 资料库、应用与资料库并行更新、慢网和失败重试场景实窗验收 |
| Bug #22 | P1 | 🟡 写后一致性方案已收敛，待真实账号验收 | 游戏内移动装备后仓库页面不更新，手动刷新可能接受旧 Profile；主进程已落盘正确快照时 Renderer 仍可能显示旧角色背包；转移、装备和配装写入后的局部状态可能被旧快照覆盖 | [任务说明](work/backlog/Bug-22-account-refresh-write-consistency.md) | 普通写接口成功后直接提交共享 Store 的局部确认态，不再登记 Pending；相同/旧 Profile 不重建页面，后续定时、回前台或手动同步的新 Profile 自然校准。配装“转移后装备”仍在主进程确认目标背包后继续。下一步验证应用内转移/装备即时更新、正常同步校准、双端最终一致和配装前置确认。 |
| Bug #23 | P1 | ✅ 阻止规则与旧库隔离已完成 | Aegis 原始刀剑大师属性 `Impact` 在 Starside 快照中曾被写成非官方字段“伤害”；导入器不能据此推断或改写为“冲击” | [任务说明](work/backlog/T20-weapon-recommendation-vault-cleanup.md) | 严格预览会阻止“伤害”等非官方值；包含 40 条历史“伤害”的旧 SQLite 保留但标记为 `unverified`，不再参与推荐、保护或整理结论，也不会被自动改写 |
| Bug #24 | P1 | 🟡 代码与完整包已修复，待实窗导入验收 | 武器推荐 CSV 导入过去只检查表头、列数、重复键和数字格式，无法阻止不存在、错栏或非官方名称进入 SQLite；现有 31 列模板也不适合普通玩家维护 | [任务说明](work/backlog/T20-weapon-recommendation-vault-cleanup.md) | DIM 已从普通 CSV 拆为原生 Wishlist 数据链；普通玩家改用 13 列简表并执行合并更新，异常行单独忽略。完整包生成器现将“换弹/操控/稳定”等来源简称转换为当前官方名称，过滤已划除/已删除内容，禁止过滤后的原始字段回填；应用补齐 `rail`、`bolt` 等特殊武器官方栏位识别。2026-09-05 01:10 重新生成的完整包含 1530 条人工记录，严格栏位诊断为 0 异常。下一步在重启后的应用中导入该文件，确认显示 1530/1530、0 行忽略并执行完整替换。 |
| Bug #25 | P1 | ✅ 已修复，待启动验收 | Desktop 开发模式窗口黑屏，Renderer 未渲染首页 | — | `useDesktopProductShell` 重构时遗漏 `canRefreshAccount` 定义，导致 `ReferenceError` 阻断 React 根节点渲染；已补回账号配置状态派生值，并清理残留 Electron/Vite 进程后重新启动。下一步确认用户侧开发启动与已打包版本均可正常显示 |
| Bug #26 | P1 | ✅ 问题链路已移除，待真实账号验收 | Bungie 写入后的后台确认任务虽已完成，但装备详情以 `surfaceFeedback: false` 注册的静默任务不会消费成功/失败终态，账号页可能一直显示“确认中” | — | 装备详情、仓库和普通配装写入不再注册静默确认任务；接口成功后直接提交局部状态并显示一次成功结果，因此不存在任务终态未消费、75 秒兜底或“确认中”残留。下一步复测单件装备与最高光等入口，确认完成后没有后台账号写任务。 |
| Bug #27 | P1 | 🟡 代码修复完成，待实窗验收 | 推荐数据管理弹窗打开后自动展开第一个来源的规则明细，最多加载 200 条，导致窗口过长 | — | 来源管理默认只显示来源汇总；规则明细改为点击“管理规则”后按来源展开，关闭规则区后不再自动加载。下一步确认弹窗初始高度、来源切换和规则搜索仍可用 |
| Bug #28 | P1 | 🟡 代码修复完成，待真实账号验收 | 中文推荐库恢复可用后，仓库仍复用此前只包含 DIM 的匹配缓存，导致推荐卡只展示 DIM | — | 匹配缓存算法版本已递增，中文推荐可用性或匹配语义变化后会强制重新核对，避免旧缓存遮蔽 Aegis、LGpig、YXCRALLXY 和 Sayalarry。下一步重启应用并执行一次仓库同步确认四个人工来源重新出现 |
| Bug #29 | P1 | 🟡 代码修复完成，待真实账号验收 | 完整武器 Roll 因推荐 Hash 未解析被误标为“数据未读取”，且武器卡、推荐列表、同名 Roll、详情与 DIM 使用多套状态描述 | [任务说明](work/backlog/T20-weapon-recommendation-vault-cleanup.md) | 匹配器改为优先使用完整本件栏位事实：推荐名称未解析出当前版本 Hash 时继续按规范化官方名称比较；目标栏完整但未命中、或完整 Roll 没有该栏位时明确判为“不符”。逐栏统一 4 种状态，推荐列表统一 5 类结果，运行状态移除“待核对”，缓存算法升级到 10。下一步用暗夜魅影光等 413/324 和无起源栏老版本验证汇总、同名 Roll 与详情一致 |
| Bug #30 | P1 | 🟡 代码修复完成，待真实账号验收 | 武器详情推荐区只有当前启用或当前版本池内的部分 Perk 有图标；未选中的本件 Perk、跨版本推荐 Perk 和大师属性大量显示占位 | [任务说明](work/backlog/T20-weapon-recommendation-vault-cleanup.md) | 账号 Roll 直接保留官方图标与说明；匹配时也会用当前资料库补齐旧账号缓存，人工推荐同时加载规则关联版本的完整 Perk 池，大师属性复用去阶级前缀后的官方语义名称；匹配缓存算法升级到 11。下一步重启应用，用冰冷拒绝等跨版本实例确认来源要求与本件拥有的图标、命中和当前状态完整显示 |
| Bug #31 | P1 | 🟡 滚动与推荐筛选热路径已修复，待实窗验收 | 浏览装备筛选层级过多、位置与保护筛选重复；选择来源后仓库上下文、全局 Shell、推荐来源读取和手写虚拟网格形成反馈链，导致整个应用持续重渲染和明显卡顿 | [任务说明](work/backlog/T20-weapon-recommendation-vault-cleanup.md) | 模糊的“符合 / 部分符合 / 未符合”已替换为核心 Perk 与完整命中的精确 `x/y`，DIM 显示最佳组合 `x/y`；推荐 revision 变化时一次生成实例轻量事实索引，筛选与计数不再遍历来源规则。200 件以内稳定挂载并由 `content-visibility` 裁剪，长列表滚动时不读取布局或观察网格自身。下一步用约 1000 件武器账号切换来源和命中数量并连续上下滚动，确认各级数量闭合、方向键定位正常且应用保持响应。 |
| Bug #32 | P1 | 🟡 代码修复完成，待实窗验收 | 账号每 10 分钟自动同步和回到前台补同步被移除，状态栏却仍在 90 秒后误报“游戏数据延迟” | — | 已恢复应用可见期间每 10 分钟静默同步，以及回到前台、网络恢复且数据超过 2 分钟时补同步；不因切换菜单触发请求。普通快照超过 12 分钟显示“待同步”，只有写操作确认超过 90 秒显示“游戏数据延迟”。下一步保持应用前台超过 10 分钟、最小化超过 2 分钟后恢复，并在断网/恢复网络场景验收。 |
| Bug #33 | P1 | 🟡 根因链路已切断，待真实账号验收 | 仓库写后确认期间整个应用卡顿，历史 Pending 不退出并持续累加；顶部、同步横幅和操作提示重复且数量不一致 | [任务说明](work/backlog/T20-weapon-recommendation-vault-cleanup.md) | 普通写操作已移除逐操作五轮 Profile 确认、75 秒终态刷新和全局账号同步状态；API 成功只更新受影响实体，静默账号推送不再制造重复成功横幅。配装前置依赖仍使用主进程针对性背包确认。根 Shell 后台任务窄订阅和虚拟网格稳定测量继续生效。下一步连续执行取出/加锁/存入并同时滚动，确认 API 完成后立即空闲、没有 8MB 写后快照重建且应用持续响应。 |
| Bug #34 | P1 | 🟡 代码修复完成，待实窗验收 | 应用更新或资料库任务等待重试时，右下角浮窗永久显示且无法收起，并遮挡底部快捷键和操作栏 | — | 后台任务状态继续由顶部状态栏持续保留；浮窗改为顶部栏下方的可收起临时通知，收起不取消任务，运行和等待重试状态会自动收起，失败与阻断允许手动确认；设置页保留完整详情。下一步在应用更新超时、资料库下载、等待重试、失败和窄窗口场景验收。 |
| Bug #35 | P2 | 🟡 代码修复完成，待实窗验收 | 首页手动刷新使用含混的“公开情报”文案，缺少刷新图标、明确忙碌状态和全局更新时间，容易被误认为与绿色主操作不一致 | — | 正常刷新保留次级权重并统一为“刷新首页情报”，Desktop 与 Web 共用刷新图标和忙碌状态；首页摘要显示上次成功刷新时间，失败 Callout 独占绿色“重新读取首页情报”。下一步在亮色、暗色及 `1280px / 980px / 760px` 下验收按钮、长文案和更新时间布局。 |
| Bug #36 | P2 | 🟡 代码修复完成，待实窗验收 | 左侧账号摘要提供手动收起按钮，但应用一级菜单数量较少，收起后会隐藏账号与导航文案，增加理解成本且实际价值有限 | — | 已删除手动收起入口和折叠状态；桌面侧栏固定展开，窄窗口继续由响应式布局自动切换为紧凑栏。下一步在 Desktop / Web 及 `1280px / 980px / 760px` 下确认侧栏宽度、账号摘要和导航可访问名称正常。 |
| Bug #37 | P1 | 🟡 代码修复完成，待真实账号验收 | 首页“本周活动焦点”展示的是焦点活动事件，不是玩家需要的当前元素激涌；同时缺少生效范围说明，容易被误解为全局 PvE Buff | — | 已改为从登录账号 `CharacterActivities.modifierHashes` 与当前 Manifest 汇总本周元素激涌，明确仅对活动详情列出对应修饰词的 PvE 内容生效并排除明确 PvP 活动；未登录、读取失败、数据过期或没有已确认激涌时不显示。首页缓存升级为 v6，旧焦点活动缓存自动失效。下一步用真实账号确认当前“电弧 / 冰影”及下次周重置后的组合正确更新。 |
| Bug #38 | P1 | ✅ 已修复，本地 CI 通过 | 资料库社区推荐计数字段未同步到 App ViewModel，且账号写操作测试仍断言已移除的后台确认与缺 Patch 全量刷新链路，导致本地 CI 失败 | — | 已补齐资料库推荐计数契约、修正 UI 非空收窄，并将高风险账号写入测试更新为“接口成功即接受局部 Patch、缺 Patch 等待后续账号同步”的当前行为；同时修正单件装备缺 Patch 时误报“页面已更新”的文案。 |

## 说明

- 已移除独立本地攻略库；攻略链接、正文和 AI 整理结果统一进入配装页生成未保存草稿，旧攻略 JSON 只作为遗留备份数据保留。
- UI 只维护 `packages/ui` 的共享产品实现；Markdown 合同记录稳定约束，Web 用于快速预览，Desktop 用于真实功能验收。
- UI 合同静态门禁覆盖稳定标记、表面枚举、排版、层级、菜单主题色和选中态方向线，避免菜单实现重新取得共享视觉职责。
- 新增 Bug 必须使用全局唯一编号并在本文件中登记。
